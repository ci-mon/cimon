@page "/monitor/{monitorId?}"

<PageTitle>CI @MonitorId</PageTitle>

@code {
    [Parameter]
    public string? MonitorId { get; set; }
    
    protected override void OnInitialized()
    {
        MonitorId = MonitorId ?? "default";
    }

}